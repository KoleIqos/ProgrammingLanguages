import java.util.List;
import java.util.Random;
import java.util.stream.Collector;
 class Student {
	 private String name;
	 private int indexNo;
	 private int age;
	 
	 public String getName() {
		 return name;
	 }
	 public int getIndexNo() {
		 return indexNo;
	 }
	 public int getAge() {
		 return age;
	 }
	 public Student setname(String name)
	 {
		 this.name = name;
		 return this;
	 }
	public Object setIndexNo(int indexNo2) {
		// TODO Auto-generated method stub
		return null;
	}
	
 
 class Demo {
	 public Student createStudent(String name, int indexNo, int age) {
		 return ((Object) new
	 Student().setname(name).setIndexNo(indexNo)).setAge(age);
	 }
	 
	public void printvalues(Student s) {
 }
	{
 System.out.println("Name: " + s.getName() + "Index: " + s.getIndexNo() + " Age: " + s.getAge());
 
 public static void main(String[] args) {
	 List<String> names = List.of("Kole", "Marche", "Raljo", "CHICHO", "Aron");
	 Demo demo = new Demo();
	 Random random = new Random();
	 
	 List<Students> students = name.stream().map(name -> demo.createStudent(name, random.nextInt(10000) + 1, random.nextInt(13) + 18)).collect(Collectors.toList());
	 students.forEach(demo::printValues);
 }
 }
 
